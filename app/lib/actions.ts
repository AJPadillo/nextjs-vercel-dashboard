'use server'

// Marcar que todas las funciones que se exportan son funciones de servidor
// y no se pueden usar en el cliente. Esto es importante para evitar errores de compilación

export async function createInvoice(formData: FormData) {
    
}